services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: voyant_l10n_hub
      POSTGRES_USER: voyant
      POSTGRES_PASSWORD: voyant
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
